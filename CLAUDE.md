@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs are tracked in GitHub Issues for `juliandavidmr/news-verifier`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository with `CONTEXT.md` and system-wide ADRs under `docs/adr/`. See `docs/agents/domain.md`.

### Deployment

The GitHub repository is connected to the Vercel project `news-verifier`; pushes to `master` deploy automatically to production. See `docs/agents/deployment.md`.
