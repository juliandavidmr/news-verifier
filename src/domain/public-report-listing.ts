export type PublicReportListing = {
  shortId: string;
  title: string;
  sourceHostname: string;
  completedAt: string;
};

export type IndexableReport = {
  shortId: string;
  completedAt: string;
};
